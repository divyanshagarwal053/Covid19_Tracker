<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ferid.app.a19tacker">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.19Tacker">
        <activity android:name=".StateDistrictData"></activity>
        <activity android:name=".EachStateData" />
        <activity android:name=".EachDistrictData" />
        <activity android:name=".EachCountryData" />
        <activity android:name=".StateData" />
        <activity android:name=".WorldData" />
        <activity android:name=".DistrictData" />
        <activity android:name=".CountryData" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity2" />
    </application>

</manifest>